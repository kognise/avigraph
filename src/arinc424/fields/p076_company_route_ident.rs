use crate::trimmed_str_parseable;

trimmed_str_parseable! {
    /**
    The "Company Route Ident" field identifies each unique route between origination and
    destination.

    This field is determined by the customer.
    */
    pub CompanyRouteIdent: 10;
}
